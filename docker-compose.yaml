services:
  taverna:
    build: .
    ports:
      - "2948:2948"
    volumes:
      - ${VAULT_PATH:?Set VAULT_PATH to your vault path, e.g. export VAULT_PATH=$HOME/my-vault}:/vault:ro
    environment:
      - VAULT_PATH=/vault
    command: serve
